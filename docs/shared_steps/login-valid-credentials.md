# Shared Step: Login with valid credentials

**Purpose**: Reusable sequence for logging into the application with predefined valid credentials.

**Steps**:
1. **Action**: Navigate to login page
   **Expected Result**: Login page is displayed
2. **Action**: Enter valid username and password
   **Data**: `username: {username}, password: {password}`
   **Expected Result**: User is redirected to dashboard